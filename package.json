{
  "name": "telemetry-export-receipts",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "dev:server": "cargo run",
    "build": "vite build",
    "test": "vitest run && cargo test",
    "test:e2e": "playwright test",
    "check": "tsc --noEmit && cargo clippy --all-targets -- -D warnings"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.12.1",
    "@playwright/test": "1.58.2",
    "@types/node": "22.12.0",
    "typescript": "5.9.2",
    "vite": "7.3.6",
    "vitest": "3.2.7"
  }
}
